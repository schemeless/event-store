{
  "name": "@schemeless/event-store-cash-account-example",
  "version": "0.1.0",
  "typescript:main": "src/index.ts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "../../node_modules/.bin/tsc -w",
    "clean": "../../node_modules/.bin/rimraf ./dist",
    "test": "yarn run compile && ../../node_modules/.bin/jest --passWithNoTests",
    "compile": "yarn workspace @schemeless/event-store-types compile && yarn workspace @schemeless/event-store-core compile && yarn workspace @schemeless/event-store-aggregate compile && yarn run clean && ../../node_modules/.bin/tsc",
    "prepublish": "yarn run clean && yarn run compile"
  },
  "dependencies": {
    "@schemeless/event-store-core": "^0.1.0",
    "@schemeless/event-store-aggregate": "^0.1.0",
    "@schemeless/event-store-types": "^5.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^14.14.9",
    "jest": "^30.2.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^29.4.6",
    "typescript": "^4.1.2"
  }
}
