# Cash Account Reference

Reference aggregate for the v6 redesign.

It demonstrates:

- command handling via `event-store-aggregate`
- read-model rebuild via `event-store-core`
- snapshot-assisted hydrate
- OCC failure on concurrent writes
